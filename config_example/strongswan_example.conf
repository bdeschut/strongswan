# /opt/strongswan/strongswan.conf
charon {
    dns_resolver {
        interval = 300
    }

    # Use default or tuned retransmits (do NOT set to 0)
    retransmit_tries = 5
    retransmit_timeout = 4.0
    retransmit_base = 1.8
}
